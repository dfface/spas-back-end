<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spas.backend.mapper.UserMapper">
    <select id="selectUserByEmail" resultType="User">
        SELECT u.id, u.name, u.salt, u.password, u.position, u.email, u.phone, u.office_id, u.department_id, u.avatar
        FROM user as u WHERE email = #{email} AND is_deleted = b'0'
    </select>
</mapper>
